static:
  build: static
  volumes:
    - /opt/static

web:
  build: nginx
  ports:
    - "8080:8080"
  volumes_from:
    - static
  links:
    - elkx

elkx:
  build: elk
  volumes_from:
    - static
  ports:
    - "8180:8080"
  expose:
    - "8050"
    - "8080"