static:
  build: static
  volumes:
    - /opt/static

web:
  build: nginx
  ports:
    - "8080:8080"
  volumes_from:
    - static

logstash:
  build: logstash
  volumes_from:
    - static
  ports:
    - "8050:8050"